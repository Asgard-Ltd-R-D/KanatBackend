services:
  # Database Services
  questdb-dev:
    image: kanatbackend-questdb
    build:
      context: ./QuestDB
    pull_policy: never
    container_name: questdb-packets-dev
    ports:
      - "8812:8812" # HTTP/Web Console
      - "9009:9009" # PostgreSQL wire protocol
      - "9000:9000" # QuestDB ILP
      - "9003:9003" # Health and metrics
    environment:
      - QDB_METRICS_ENABLED=TRUE
      - QDB_PG_USER=quest
      - QDB_PG_PASSWORD=quest
      - QDB_PG_DATABASE=PacketDBDev
    volumes:
      - questdb_data:/root/.questdb/db
      - questdb_logs:/root/.questdb/log
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9003"]
      interval: 10s
      timeout: 10s
      retries: 3

  postgresdb-dev:
    image: postgres:15-alpine
    container_name: postgres-range-dev
    ports:
      - "5432:5432" # PostgreSQL port
    environment:
      - POSTGRES_DB=pdb
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d pdb"]
      interval: 10s
      timeout: 10s
      retries: 3

  seq:
    image: datalust/seq:latest
    container_name: seq-dev
    restart: unless-stopped
    environment:
      ACCEPT_EULA: "Y"
      SEQ_FIRSTRUN_ADMINPASSWORD: "admin"
      SEQ_FIRSTRUN_ADMINUSERNAME: "admin"
      SEQ_BINDINGS__0__URI: "http://0.0.0.0:80"
      SEQ_BINDINGS__1__URI: "http://0.0.0.0:5341"
    ports:
      - "5341:80"      
      - "5342:5341"
    volumes:
      - seq-data:/data 

volumes:
  questdb_data:
  questdb_logs:
  postgres_data:
  seq-data: